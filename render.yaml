services:
  - type: web
    name: imperivm-online-relay
    runtime: node
    plan: free
    rootDir: online-bridge/relay-server
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /healthz
    autoDeploy: false
